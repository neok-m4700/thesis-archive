!	*** ABHIJIT MUKHERJEE ***
C2345678902234567890323456789042345678905234567890623456789072
      SUBROUTINE INPUT3D
	IMPLICIT NONE
	INCLUDE "VAR3D.DEC"
	OPEN (10, FILE='in')
	READ(10,*)
	READ(10,*) START, LEN, HEI, WID, L, M, N, OT
	READ(10,*)
	READ(10,*) DT, ITMAX, ITERMAX, EPS, CONANG, DYB
	READ(10,*)
	READ(10,*) REL, PRL, EOL, JAL, RADIUS, BETAL
	READ(10,*)
	READ(10,*) RHOR, MUR, CONDR, CPR, L0, T0, FAC 
	READ(10,*)
	READ(10,*) TWALL, TINF, DELTA, MIC, ALPHAVEL, OMEGAVEL
	CLOSE(10)		
	RETURN
	END
